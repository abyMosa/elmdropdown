# Elm MultiSelect Dropdown
- clone the repository
    ```
    git clone git@github.com:abyMosa/elmdropdown.git
    ```
- install packages 
    ```
    npm install
    ```
- run the app
    ```
    npm start
    ```